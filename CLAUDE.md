# gridstrap

Reusable React grid component library (Bootstrap-style, 12-column).

## Stack
- TypeScript, React 19 peer dep
- tsup (build), sass (styles), eslint + prettier
- yarn 4

## Commands
- `yarn build` — compile TS + SCSS to `dist/`
- `yarn lint` — eslint
- `yarn format` — prettier
